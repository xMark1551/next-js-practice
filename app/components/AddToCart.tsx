"use client";

const AddToCart = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("clicked");
        }}
      >
        sdf
      </button>
    </div>
  );
};

export default AddToCart;
